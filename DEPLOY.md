# BOBO油耗 微信小程序部署指南

## 前置条件检查清单

在开始部署之前，请确认以下条件已满足：

| # | 条件 | 状态 |
|---|---|---|
| 1 | 已注册微信小程序账号（个人或企业） | ☐ |
| 2 | 已获取小程序 AppID（格式：`wx` 开头 18 位字符） | ☐ |
| 3 | 已完成微信实名认证（个人）或企业认证 | ☐ |
| 4 | 电脑已安装 [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html) | ☐ |
| 5 | API 服务器 `https://youhao.hongbuo007.cn` 已配置有效 HTTPS 证书 | ☐ |
| 6 | 本地已安装 Node.js 18+ | ☐ |

---

## 第一步：注册微信小程序（如已注册可跳过）

1. 打开 [微信公众平台](https://mp.weixin.qq.com/)
2. 点击右上角「立即注册」→ 选择「小程序」
3. 按提示填写邮箱、密码，激活账号
4. 选择主体类型：
   - **个人**：只需身份证+手机号验证，适合个人使用
   - **企业**：需营业执照，功能更全（支持微信支付等）
5. 登录后进入「开发管理」→「开发设置」，复制 **AppID**

---

## 第二步：配置项目 AppID

将上一步获取的 AppID 填入项目配置文件：

### 2.1 修改 `project.config.json`

```bash
# 编辑文件，找到 "appid" 字段
# 将 "your-wechat-appid" 替换为你的真实 AppID
```

修改前：
```json
"appid": "your-wechat-appid",
```

修改后（示例）：
```json
"appid": "wx1234567890abcdef",
```

### 2.2 修改小程序名称（`src/app.config.ts`）

找到第 16 行，将导航栏标题从 `bobo油耗` 改为 `BOBO油耗`：

```typescript
navigationBarTitleText: 'BOBO油耗',
```

---

## 第三步：配置微信服务器域名

登录 [微信公众平台](https://mp.weixin.qq.com/) → 开发管理 → 开发设置 → 服务器域名：

| 配置项 | 域名 |
|---|---|
| **request 合法域名** | `https://youhao.hongbuo007.cn` |
| **socket 合法域名** | （留空，本项目不使用 WebSocket） |
| **uploadFile 合法域名** | （留空） |
| **downloadFile 合法域名** | （留空） |

> ⚠️ 注意：
> - 域名必须使用 **HTTPS**，不支持 HTTP 和 IP 地址
> - 每月仅可修改 5 次，请确认无误后保存
> - 配置后约 5-10 分钟生效
> - 开发阶段可在微信开发者工具中勾选「不校验合法域名」临时跳过

---

## 第四步：编译项目

在项目根目录执行：

```bash
cd bobo-oil-miniapp

# 安装依赖（首次）
npm install

# 编译微信小程序
npm run build:weapp
```

编译成功后，产物在 `dist/` 目录。

> 如遇编译错误，请确保 Node.js 版本 ≥ 18，可执行 `node -v` 检查。

---

## 第五步：导入微信开发者工具

1. 打开 **微信开发者工具**
2. 点击「+」或「导入项目」
3. 填写信息：

   | 字段 | 值 |
   |---|---|
   | 项目名称 | BOBO油耗 |
   | 目录 | 选择项目根目录（不是 `dist/`） |
   | AppID | 你的小程序 AppID |
   | 开发模式 | 小程序 |
   | 后端服务 | 不使用云服务 |

4. 点击「确定」，工具会自动识别 `project.config.json` 并加载 `dist/` 目录

---

## 第六步：预览与调试

### 6.1 真机预览
- 点击工具栏「预览」按钮
- 用微信扫描二维码
- 在手机上体验小程序

### 6.2 调试要点

| 检查项 | 预期结果 |
|---|---|
| 登录页 | 显示「BOBO油耗」标题，微信一键登录正常 |
| 仪表盘 | 如无数据，显示空状态引导 |
| 添加加油记录 | 表单正常填写、提交、返回列表 |
| 统计报表 | 趋势图和月度汇总正常展示 |
| 设置页 | 数据导入导出功能正常 |

### 6.3 常见问题

| 问题 | 解决方案 |
|---|---|
| 请求失败 / 网络错误 | ① 检查服务器域名是否配置 ② 开发工具中勾选「不校验合法域名」 |
| 登录失败 | 检查服务器 `server/routes/auth.js` 中微信 AppID 和 AppSecret 是否正确 |
| TabBar 图标不显示 | 图标文件需为 40x40px 的 PNG，检查 `src/assets/tabbar/` 目录 |
| 页面空白 | 查看控制台报错，可能是 API 返回数据格式不匹配 |

---

## 第七步：上传与发布

### 7.1 上传代码

1. 在微信开发者工具中点击右上角「上传」
2. 填写版本号（如 `1.0.0`）和备注（如「首次发布」）
3. 上传成功后，代码进入「开发版本」列表

### 7.2 设为体验版（可选但推荐）

登录 [微信公众平台](https://mp.weixin.qq.com/) → 管理 → 版本管理：
- 在「开发版本」中找到刚上传的版本
- 点击「选为体验版」
- 扫描体验版二维码，让其他用户参与测试

### 7.3 提交审核

1. 在版本管理页面，找到要发布的「开发版本」
2. 点击「提交审核」
3. 填写审核信息：

   | 字段 | 填写内容 |
   |---|---|
   | 功能页面 | 选择有核心功能的页面（如仪表盘、加油记录） |
   | 标题 | BOBO油耗 - 车辆油耗管理工具 |
   | 所在服务类目 | **工具 → 信息查询**（或 **生活服务 → 汽车服务**） |
   | 标签 | 油耗、车辆管理、加油记录 |

4. 提交后等待审核（通常 1-7 个工作日）

### 7.4 发布上线

审核通过后：
1. 在「审核版本」中点击「发布」
2. 全量发布后，用户在微信中搜索小程序名称即可使用

---

## 第八步：后续更新流程

每次代码更新后：

```bash
# 1. 拉取最新代码
git pull origin master

# 2. 重新编译
npm run build:weapp

# 3. 在微信开发者工具中上传新版本
# 4. 提交审核 → 审核通过 → 发布
```

---

## 补充：服务器端配置

确保后端服务器已正确配置微信登录：

### 检查 `server/routes/auth.js`

```javascript
// 微信小程序 AppID 和 AppSecret
const APP_ID = 'your_appid';        // ← 替换为真实 AppID
const APP_SECRET = 'your_secret';   // ← 替换为真实 AppSecret
```

这两个值在 [微信公众平台](https://mp.weixin.qq.com/) → 开发管理 → 开发设置 中获取。

### 检查 HTTPS 证书

```bash
# 在飞牛NAS上检查证书状态
docker compose logs bobo-oil | grep -i ssl
```

如果 API 地址使用自签名证书，小程序将无法访问。建议使用 Let's Encrypt 免费证书或云服务商提供的 SSL 证书。

---

## 快速命令参考

```bash
# 编译
cd /workspace/bobo-oil-miniapp && npm run build:weapp

# 开发模式（实时编译）
npm run dev:weapp

# 检查 Node 版本
node -v

# 查看 Git 状态
git status && git log --oneline -5
```
